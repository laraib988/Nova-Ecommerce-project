# Fix Hostinger Deployment Errors (ChunkLoadError + Preload Warnings)

## Current Status: [0/10] ⏳

### 1. ✅ Create this TODO.md (tracking progress)
### 2. 🧪 Test current build locally 
   `npx serve -s build -l 3000` → http://localhost:3000 → click featured product. Note any chunk errors.
### 3. 🔄 Fresh rebuild
   ```
   cd "Nova-Ecommerce-project-"
   npm install
   npm run build
   ```
   Verify `build/static/js/` has ~50+ chunk files (no missing).
### 4. 📤 Upload FULL build/ to Hostinger /shop/
   - ZIP entire `build/` folder
   - Upload/overwrite `/shop/` on Hostinger
   - Clear cache
### 5. 🧪 Test uploaded build (no code changes)
   Visit site → click featured → chunk error fixed?
### 6. ✏️ Fix preload warning (Home.js)
   Edit `src/pages/Home.js`: remove `crossOrigin: 'anonymous'`
### 7. ✏️ Fix .htaccess for /shop/ subdir
   Edit `public/.htaccess`: `RewriteBase /shop/`
### 8. ✏️ Add ErrorBoundary.js (crash protection)
   Create `src/components/ErrorBoundary.js`
### 9. 🔄 Rebuild + re-upload after fixes
   `npm run build` → upload build/
### 10. ✅ Final test + cleanup
   Test all: preload gone? ProductDetail loads? Mark [10/10] ✅

**Next step:** Reply "TODO created. Run step 2 (local serve test) + paste console errors?"

**Notes:**
- Chunk 2293 missing → incomplete upload. Full rebuild fixes.
- Preload: crossorigin mismatch on Unsplash (public CDN).
- Hard refresh (Ctrl+Shift+R) after uploads.

---
*Generated by BLACKBOXAI - Track: Update this file after each step.*
